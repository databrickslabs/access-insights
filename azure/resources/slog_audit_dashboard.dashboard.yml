resources:
  dashboards:
    slog_audit_dashboard:
      display_name: "slog_audit_dashboard_dab"
      warehouse_id: ${var.warehouse_id}
      file_path: ../dashboards/slog_audit_dashboard.lvdash.json
